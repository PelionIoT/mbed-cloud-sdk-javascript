<% enums.forEach(function(_enum) { %>
export type <%= _enum.enum_name.pascal %> = <% _enum.values.forEach(function(value) { %> "<%= value %>"<% if (_enum.values.indexOf(value) < _enum.values.length - 1) { %> |<% } %><% }); %>;
<% }); %>