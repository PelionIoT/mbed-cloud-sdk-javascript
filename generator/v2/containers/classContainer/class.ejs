/**
*<%= classContainer.description %>
*/
export class <%= classContainer.name %><%= classContainer.extendsClass ? " extends " + classContainer.extendsClass : "" %><%= classContainer.implementsClasses.length > 0 ? " implements " + classContainer.implementsClasses.join() : "" %> {
}