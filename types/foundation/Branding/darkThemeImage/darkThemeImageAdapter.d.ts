import { Adapter } from "../../../common/adapter";
import { DarkThemeImage } from "./darkThemeImage";
/**
 *DarkThemeImage adapter
 */
export declare class DarkThemeImageAdapter extends Adapter {
    /**
     * fromApi
     * @param data - data
     * @param instance - instance
     */
    static fromApi(data: any, instance?: any): DarkThemeImage;
}
