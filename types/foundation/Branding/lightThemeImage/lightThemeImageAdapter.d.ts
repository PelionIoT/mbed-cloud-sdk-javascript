import { Adapter } from "../../../common/adapter";
import { LightThemeImage } from "./lightThemeImage";
/**
 *LightThemeImage adapter
 */
export declare class LightThemeImageAdapter extends Adapter {
    /**
     * fromApi
     * @param data - data
     * @param instance - instance
     */
    static fromApi(data: any, instance?: any): LightThemeImage;
}
