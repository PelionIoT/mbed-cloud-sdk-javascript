import { Adapter } from "../../../common/adapter";
import { ActiveSession } from "./activeSession";
/**
 *ActiveSession adapter
 */
export declare class ActiveSessionAdapter extends Adapter {
    /**
     * fromApi
     * @param data - data
     * @param instance - instance
     */
    static fromApi(data: any, instance?: any): ActiveSession;
}
